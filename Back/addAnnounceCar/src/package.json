{
  "name": "add",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "start": "node ./dist/server.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "eslint ./src/**"
  },
  "author": "me",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.0"
  }
}
